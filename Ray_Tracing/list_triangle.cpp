#include "list_triangle.h"

//on regarde si on traverse un triangle, si oui on renvoie le premier qu'on traverse et le pts d'intersection
bool List_triangle::is_intersect(const Rayon& r,Triangle& t,Vec3& inter){
    return true;
}
